#include <stdio.h>

/**
 * main - Prints numbers from 0 - 9.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 40; i < 58; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
